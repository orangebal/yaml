# ------------------- Deployment ------------------- #
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample
  namespace: default
spec:
  selector:
    matchLabels:
      app: sample
  replicas: 2
  revisionHistoryLimit: 10
  template:
    metadata:
      labels:
        app: sample
    spec:
      containers:
      - name: sample
        image: 
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          protocol: TCP
        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
        ports:
        - containerPort: 80
---

# ------------------- Service ------------------- #

apiVersion: v1
kind: Service
metadata:
  name: sample
  namespace: default
  labels:
    app: sample
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: sample
